name: 🏆 Daily Sports Guide

on:
  schedule:
    - cron: '30 12 * * *'  # Runs once a day at 7:30 AM EST (12:30 UTC)
  workflow_dispatch:       # Allows you to run it manually whenever you want

jobs:
  post_guide:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 📦 Install dependencies
        run: pip install requests

      - name: 🏟️ Run Sports Guide Script
        env:
          # Use your existing Live Webhook or a new one if you prefer
          DISCORD_LIVE_WEBHOOK: ${{ secrets.DISCORD_LIVE_WEBHOOK }}
        run: python scripts/sport_guide.py
